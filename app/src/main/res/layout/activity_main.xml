<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                xmlns:tl="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:fitsSystemWindows="true">
    <!--  app:dayNightBackgroundAttr="dayNightBackground"-->
    <FrameLayout
        android:background="@color/white"
        android:id="@+id/fl_body"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        />
    <com.flyco.tablayout.CommonTabLayout
        android:id="@+id/tab_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/indecttabstext"
        android:padding="5dp"
        tl:tl_iconHeight="20dp"
        tl:tl_iconWidth="20dp"
        tl:tl_indicator_color="@color/gray"
        tl:tl_indicator_height="0dp"
        tl:tl_textSelectColor="@color/red"
        tl:tl_textUnselectColor="@color/light_gray"
        tl:tl_textsize="11sp"
        tl:tl_underline_height="0dp"/>
   <!-- <RadioGroup
        android:id="@+id/rg_main_menu"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@color/gray"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <RadioButton
            style="@style/MainMenuRadioButton"
            android:drawableTop="@drawable/tab_newbook_selector"
            android:text="新书推荐"
            android:id="@+id/radioButton1"/>

        <RadioButton
            style="@style/MainMenuRadioButton"
            android:drawableTop="@drawable/tab_libabry_selector"
            android:text="政协书库"
            android:id="@+id/radioButton2"/>

        <RadioButton
            style="@style/MainMenuRadioButton"
            android:drawableTop="@drawable/tab_readbook_selector"
            android:text="委员读书"
            android:id="@+id/radioButton3"/>

        <RadioButton
            style="@style/MainMenuRadioButton"
            android:drawableTop="@drawable/tab_mybooks_selector"
            android:text="我的书库"
            android:id="@+id/radioButton4"/>


    </RadioGroup>-->


</LinearLayout>